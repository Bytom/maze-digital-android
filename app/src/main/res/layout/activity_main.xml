<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="false"
    xmlns:app="http://schemas.android.com/apk/res-auto">





    <FrameLayout
        android:id="@+id/main_frame"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@+id/rg"
        app:layout_constraintTop_toTopOf="parent" />

    <RadioGroup
        android:id="@+id/rg"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="@color/base_white"
        android:checkedButton="@id/rb_discover"
        android:elevation="@dimen/common_margin"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent">

        <RadioButton
            android:id="@+id/rb_discover"
            style="@style/RadioButtonStyle"
            android:background="@color/transparent"
            android:drawableTop="@drawable/selector_tab_discover"
            android:layout_gravity="center_vertical"
            android:paddingTop="18dp"
            />

        <!--<RadioButton-->
        <!--android:id="@+id/rb_rank"-->
        <!--style="@style/RadioButtonStyle"-->
        <!--android:background="@color/transparent"-->
        <!--android:drawableTop="@drawable/selector_tab_rank"-->
        <!--android:paddingTop="@dimen/bytom_5dp"-->
        <!--android:text="@string/mov_exchange" />-->

        <ImageView
            android:id="@+id/rb_create"
            android:layout_marginBottom="12dp"
            android:background="@drawable/create_art"
            android:layout_gravity="bottom"

            android:layout_width="74dp"
            android:layout_height="68dp"/>



        <RadioButton
            android:id="@+id/rb_mine"
            style="@style/RadioButtonStyle"
            android:background="@color/transparent"
            android:paddingTop="18dp"

            android:drawableTop="@drawable/selector_tab_mine"
            />



    </RadioGroup>




</androidx.constraintlayout.widget.ConstraintLayout>